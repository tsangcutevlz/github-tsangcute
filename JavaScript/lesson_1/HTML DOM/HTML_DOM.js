// DOM (doccument object model ): có 3 thành phần

/*
    1. Elements: 
    - ID, class, tag ( div, h1, p, ..), 
    - CSS selector: querySelector, querySelectorAll
    - HTML selection: các thẻ html 
    2. Attribute:
    3. Text
*/

// Node: elements, attribute, text 

// ----------------------------------
// HTML DOM      VS        DOM API

// JAVASCRIPT: Browser | Server (NODEJS)

// Broswer: HTML -> DOM -> WEB API ( Application programming interface )

console.log(document); // #document: đại diện toàn bộ website
